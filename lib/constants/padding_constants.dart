import 'package:flutter/cupertino.dart';

class PaddingConstants {
  static const lowPaddingSize = 10.0;
  static const medPaddingSize = 17.5;
  static const highPaddingSize = 25.0;

  static const lowPadding = EdgeInsets.all(lowPaddingSize);
  static const medPadding = EdgeInsets.all(medPaddingSize);
  static const highPadding = EdgeInsets.all(highPaddingSize);
}
